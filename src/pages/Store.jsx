import styled from "styled-components";
import StoreContainer from "../containers/StoreContainer";

const Store = () => {
  return (
    <StorePageBlock>
      <StoreContainer />
    </StorePageBlock>
  );
};

const StorePageBlock = styled.div``;
export default Store;
