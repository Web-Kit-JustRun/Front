import styled from "styled-components";
import PerchaseListContainer from "../containers/PurchaseListContainer";

const PurchaseList = () => {
  return (
    <PurchaseListBlock>
      <PerchaseListContainer />
    </PurchaseListBlock>
  );
};

const PurchaseListBlock = styled.div``;
export default PurchaseList;
