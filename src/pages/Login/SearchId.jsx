import styled from "styled-components";
import SearchIdContainer from "../../containers/Login/SearchIdContainer";

const SearchId = () => {
  return (
    <SearchIdPageBlock>
      <SearchIdContainer />
    </SearchIdPageBlock>
  );
};

const SearchIdPageBlock = styled.div``;
export default SearchId;
